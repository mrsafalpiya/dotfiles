#!/bin/sh

devid=$(xinput | grep "USB OPTICAL MOUSE" | cut -d'=' -f 2 | cut -f 1)
propid=$(xinput --list-props "$devid" | grep "libinput Accel Speed (" | \
		cut -d'(' -f2 | cut -d')' -f1)

xinput --set-prop "$devid" "$propid" -1
