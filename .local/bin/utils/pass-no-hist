#!/bin/sh

# This script disables clipmenud for a while in which we copy the password to
# our clipboard (means the password isn't saved to our clipboard history) after
# which we enable the clipmenud.
# So we can paste the password but it isn't stored in clipmenu.

passname="$1"

[ -z "$passname" ] && echo "Usage: $0 pass-name" && exit 1

clipctl disable && pass show "$passname" | xclip -r -sel c && clipctl enable
